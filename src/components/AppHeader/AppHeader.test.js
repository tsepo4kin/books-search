import { render, screen } from "@testing-library/react";

import AppHeader from "./AppHeader";

test('renders app header', () => {
  // render(<AppHeader />);
  // const linkElement = screen.getByText('Search for books');
  // expect(linkElement).toBeInTheDocument();
});
